# 🥬 Сайт «Овощи и фрукты» — Коряжма

Полностью готовый сайт для магазина свежих овощей и фруктов в городе Коряжма.

---

## 🚀 Как запустить сайт на компьютере

⚠️ **Важно:** Сайт нельзя открыть простым двойным кликом по `index.html` — браузер заблокирует загрузку данных. Нужен локальный сервер.

### Способ 1: VS Code + Live Server (рекомендуется)

1. Установите [VS Code](https://code.visualstudio.com/)
2. Откройте папку с сайтом в VS Code (`Файл` → `Открыть папку`)
3. Установите расширение **Live Server**:
   - Нажмите `Ctrl+Shift+X`
   - Найдите "Live Server" от Ritwick Dey
   - Нажмите "Install"
4. Кликните правой кнопкой на `index.html` → **"Open with Live Server"**
5. Готово! Браузер откроется автоматически (`http://127.0.0.1:5500`)

### Способ 2: Python (если установлен)

Откройте терминал в папке с сайтом и выполните:

```bash
# Windows / Mac / Linux
python -m http.server 8000
```

Затем откройте в браузере: `http://localhost:8000`

### Способ 3: Node.js

```bash
# Установите один раз
npm install -g http-server

# Запускайте в папке с сайтом
http-server
```

Откройте: `http://localhost:8080`

### Способ 4: OpenServer / XAMPP (Windows)

1. Установите [OpenServer](https://ospanel.io/) или [XAMPP](https://www.apachefriends.org/)
2. Скопируйте папку сайта в `domains` (OpenServer) или `htdocs` (XAMPP)
3. Запустите сервер
4. Откройте `http://localhost/название-папки`

---

## 📁 Структура проекта

```
/site
├── css/
│   └── style.css          # Стили сайта
├── js/
│   └── main.js            # JavaScript логика
├── data/
│   ├── products.json      # Данные о товарах (редактируйте здесь!)
│   └── content.json       # Контент сайта (тексты, контакты)
├── images/                # Папка для изображений (создайте при необходимости)
├── index.html             # Главная страница
├── catalog.html           # Каталог товаров
├── prices.html            # Прайс-лист
├── about.html             # О нас
├── contacts.html          # Контакты
└── README.md              # Эта инструкция
```

---

## 🚀 Как изменить товары, цены и фото за 5 минут

### 1. Добавление/редактирование товаров

Откройте файл `data/products.json` в любом текстовом редакторе (Блокнот, Notepad++, VS Code).

**Структура одного товара:**

```json
{
  "id": 1,
  "name": "Помидоры розовые",
  "category": "vegetables",
  "price": 189,
  "unit": "кг",
  "description": "Сочные розовые томаты, идеальны для салатов",
  "image": "https://images.unsplash.com/photo-1546470427-0d4db154ceb8?w=400&h=300&fit=crop",
  "badge": "Хит продаж",
  "inStock": true
}
```

**Что означает каждое поле:**

| Поле | Описание | Пример |
|------|----------|--------|
| `id` | Уникальный номер товара | `1`, `2`, `3`... |
| `name` | Название товара | `"Помидоры розовые"` |
| `category` | Категория (см. ниже) | `"vegetables"` |
| `price` | Цена (число без ₽) | `189` |
| `unit` | Единица измерения | `"кг"`, `"шт"`, `"пучок"` |
| `description` | Краткое описание | `"Сочные розовые томаты"` |
| `image` | Ссылка на изображение | URL картинки |
| `badge` | Бейдж (необязательно) | `"Хит продаж"`, `"Новинка"`, `""` |
| `inStock` | В наличии | `true` или `false` |

**Доступные категории:**

| ID категории | Название |
|--------------|----------|
| `vegetables` | Овощи |
| `fruits` | Фрукты |
| `berries` | Ягоды |
| `nuts` | Орехи и сухофрукты |
| `greens` | Зелень |
| `exotic` | Экзотические фрукты |

### 2. Изменение цены

Просто найдите нужный товар в `data/products.json` и измените значение `price`:

```json
"price": 199,  // Было 189, стало 199
```

### 3. Добавление нового товара

Скопируйте блок товара и вставьте в конец списка (перед закрывающей `]`):

```json
,
{
  "id": 31,
  "name": "Новый товар",
  "category": "vegetables",
  "price": 100,
  "unit": "кг",
  "description": "Описание нового товара",
  "image": "https://ссылка-на-картинку.jpg",
  "badge": "",
  "inStock": true
}
```

⚠️ **Важно:** Не забудьте поставить запятую после предыдущего товара!

### 4. Изменение изображения товара

Замените ссылку в поле `image`:

```json
"image": "https://ваша-ссылка-на-картинку.jpg",
```

**Где взять ссылки на картинки:**
- Загрузите на [imgbb.com](https://imgbb.com/) и скопируйте прямую ссылку
- Используйте [Unsplash](https://unsplash.com/) — бесплатные фото
- Загрузите в папку `images/` и укажите путь: `"image": "images/tomat.jpg"`

---

## 📞 Как изменить телефон и WhatsApp

Откройте файл `data/content.json` и найдите раздел `company`:

```json
"company": {
  "name": "Овощи и фрукты",
  "phone": "+7 (900) 123-45-67",      // Отображаемый телефон
  "phoneClean": "+79001234567",        // Для ссылки tel:
  "whatsapp": "79001234567",           // Для WhatsApp (без +)
  "email": "veggies.kor@mail.ru",
  "vk": "https://vk.com/veggies_fruit_kor",
  ...
}
```

**Измените на свои данные:**

```json
"phone": "+7 (XXX) XXX-XX-XX",
"phoneClean": "+7XXXXXXXXXX",
"whatsapp": "7XXXXXXXXXX",
```

---

## 🏠 Как изменить адрес и время работы

В том же файле `data/content.json`:

```json
"company": {
  "address": "Архангельская ул., 4 (гипермаркет)",
  "workingHours": {
    "weekdays": "09:00 — 21:00",
    "weekends": "10:00 — 20:00"
  }
}
```

---

## 🌐 Как залить сайт на хостинг

### Вариант 1: Обычный хостинг (рекомендуется)

1. **Купите домен и хостинг** (например, на reg.ru, beget.com, timeweb.com)
2. **Зайдите в панель управления** хостингом
3. **Откройте файловый менеджер** или подключитесь по FTP
4. **Загрузите все файлы** в папку `public_html` или `www`:
   - index.html
   - catalog.html
   - prices.html
   - about.html
   - contacts.html
   - папка `css/`
   - папка `js/`
   - папка `data/`
5. **Готово!** Сайт доступен по вашему домену

### Вариант 2: Бесплатный хостинг GitHub Pages

1. Создайте аккаунт на [github.com](https://github.com)
2. Создайте новый репозиторий с именем `username.github.io`
3. Загрузите все файлы в репозиторий
4. Сайт будет доступен по адресу `https://username.github.io`

### Вариант 3: Netlify (бесплатно)

1. Зайдите на [netlify.com](https://netlify.com)
2. Перетащите папку с сайтом в окно
3. Получите бесплатный адрес вида `название.netlify.app`
4. Можете подключить свой домен

---

## ⚙️ Дополнительные настройки

### Изменение SEO-текстов

В файле `data/content.json` найдите раздел `seo`:

```json
"seo": {
  "indexTitle": "Овощи и фрукты в Коряжме — свежие продукты каждый день",
  "indexDescription": "Магазин свежих овощей, фруктов...",
  ...
}
```

### Изменение ссылки на ресторан «Чайхана»

В HTML-файлах найдите ссылку с текстом "Посетить ресторан «Чайхана»" и замените `href="#"` на реальную ссылку.

### Изменение карты

В файле `contacts.html` найдите блок `<iframe>` с картой и замените на свой код виджета Яндекс.Карт:

1. Откройте [Яндекс.Карты](https://yandex.ru/maps)
2. Найдите нужный адрес
3. Нажмите «Поделиться» → «Виджет карты»
4. Скопируйте код и вставьте вместо текущего

---

## 🎨 Изменение цветов и стилей

Все цвета находятся в начале файла `css/style.css`:

```css
:root {
  --primary: #22c55e;        /* Основной зелёный */
  --primary-dark: #16a34a;   /* Тёмно-зелёный */
  --secondary: #f97316;      /* Оранжевый (акцент) */
  ...
}
```

---

## ❓ Частые вопросы

### Товары не отображаются
- Проверьте, что файл `data/products.json` доступен
- Убедитесь, что JSON валидный (проверьте на [jsonlint.com](https://jsonlint.com))
- Откройте консоль браузера (F12) и посмотрите ошибки

### Как добавить новую категорию?
Добавьте в массив `categories` в файле `data/products.json`:

```json
{
  "id": "newcategory",
  "name": "Новая категория",
  "icon": "🍅"
}
```

### Как убрать товар?
Удалите весь блок товара из `data/products.json` (от `{` до `}`), не забудьте убрать лишнюю запятую.

---

## 📱 Контакты для поддержки

Если нужна помощь с сайтом, обращайтесь к разработчику.

---

**Сделано с ❤️ для магазина «Овощи и фрукты» в Коряжме**
